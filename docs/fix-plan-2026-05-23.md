# Pathfinder Learn — Fix Plan (Stage A)

Date: 2026-05-23
Branch (proposed): `fix/pathfinder-pilot-gaps-2026-05-23` (to be cut from
`refactor/pathfinder-learn-phase-1` tip `5824dc5`).
Authoritative inputs consulted:
[docs/PRD-fix-gaps-prompt.md](PRD-fix-gaps-prompt.md),
[docs/architecture-contract.md](architecture-contract.md),
[frontend/src/learning/PathfinderLearnApp.tsx](../frontend/src/learning/PathfinderLearnApp.tsx),
all five route files under
[frontend/src/learning/routes/](../frontend/src/learning/routes/),
[frontend/src/learning/components/PathfinderPhase2.tsx](../frontend/src/learning/components/PathfinderPhase2.tsx),
[frontend/src/learning/components/PathfinderPhase3.tsx](../frontend/src/learning/components/PathfinderPhase3.tsx),
[frontend/src/learning/fixtures.ts](../frontend/src/learning/fixtures.ts),
[backend/src/learning/](../backend/src/learning/) (`models.py`, `planner.py`,
`validator.py`, `mastery.py`, `diagnostic.py`, `xapi.py`, `voice.py`,
`operations.py`, `repository.py`, `multilingual.py`,
`career/planner.py`),
[backend/src/app.py](../backend/src/app.py) (Flask app, no blueprints; flat
`@app.route`),
[CV-Ayoola-2026.md](../../CV-Ayoola-2026.md),
live Playwright snapshot of `http://localhost:5174/{home,teacher,safety}`.

> Contradiction up-front: the execution prompt links to
> `docs/PRD-pathfinder-learn.md` but **that file does not exist in the repo
> on this branch** (only `PRD-fix-gaps-prompt.md` is present). The
> architecture contract already encodes the §6/§7/§10/§11/§12 substance
> needed for Stage A, so this plan uses the architecture contract as the
> authoritative PRD surrogate and calls the gap out for explicit decision in
> Stage A.5 below.

## A.1 Root-cause matrix

| # | Gap | File(s) responsible | Root cause (one sentence) | Smallest possible change |
|---|---|---|---|---|
| F1 | Untracked working tree on `refactor/pathfinder-learn-phase-1`. | `git status` shows: modified [frontend/index.html](../frontend/index.html), modified [frontend/src/main.tsx](../frontend/src/main.tsx); untracked [frontend/src/learning/PathfinderLearnApp.tsx](../frontend/src/learning/PathfinderLearnApp.tsx), [frontend/src/learning/fixtures.ts](../frontend/src/learning/fixtures.ts), [frontend/src/learning/routes/](../frontend/src/learning/routes/), [frontend/src/learning/theme/](../frontend/src/learning/theme/), [frontend/e2e/pathfinder-routes.spec.ts](../frontend/e2e/pathfinder-routes.spec.ts) + snapshot dir. | The Phase-1 UI shell was authored locally and never committed; the branch tip `5824dc5` predates the new shell. | Cut new branch from `5824dc5`, then commit the entire tree as one logical change titled `feat(frontend): land PathfinderLearnApp shell and learning routes`. |
| F2 | Pilot KPIs are synthetic, mark-free fixture numbers. | [frontend/src/learning/fixtures.ts](../frontend/src/learning/fixtures.ts) lines 158–165 (`pilotMetrics`) consumed by [routes/TrustSafetyConsole.tsx](../frontend/src/learning/routes/TrustSafetyConsole.tsx) lines 23–25 and 264–273. No `/api/learning/kpis` route registered in [backend/src/app.py](../backend/src/app.py). | The Phase-4 `PilotKPIReport`/`compute_kpi_report` in [backend/src/learning/operations.py](../backend/src/learning/operations.py) lines 28–138 is never exposed; the UI never indicates fixture origin. | Add `GET /api/learning/kpis` (flat `@app.route`, mirroring existing app.py style) that returns `compute_kpi_report(...)` with `source: "fixture"` until live pilot snapshots exist; render a small "fixture" badge in the KPI strip when `source != "live"`. |
| F3 | Voice path exists server-side but no UI invokes it. | [backend/src/learning/voice.py](../backend/src/learning/voice.py) lines 1–73 (`FlaskSockVoiceTransportAdapter.handle_offline_frame`) has no caller. No flask-sock route registers it in `app.py`. [routes/StudentLearningHome.tsx](../frontend/src/learning/routes/StudentLearningHome.tsx) hero pill "en-NG · Yoruba voice" is decorative only. | Phase 3 shipped the adapter as a pure class; the WebSocket route and feature-flagged UI entry were deferred. | Add one flask-sock route `/ws/learning/voice` that constructs `VoiceFrame` from inbound JSON and calls `FlaskSockVoiceTransportAdapter.handle_offline_frame`; add a flag-gated "Voice check-in (beta)" button on Student Home that opens the socket and renders the returned `VoiceTransportResult` (offline fallback honoured by default). Flag `VITE_PATHFINDER_VOICE_ENABLED`, default off. |
| F4 | No compliance evidence artefact. | [evidence/phase_0..4/](../evidence/) contains only trace ZIPs; no `evidence/compliance/` directory. Architecture contract §A.4 mandates DPIA, OneRoster import, CASE adapter conformance — none of these has a signed bundle. | The compliance work was tracked as a Phase-0 manual step (`docs/PRD-fix-gaps-prompt.md` §10 Phase 0 "Stays Config / Manual"); nothing in CI produces an artefact. | Create `evidence/compliance/` with: `ndpr-uk-gdpr-dpia.md` outline, `oneroster_smoke.csv` + `test_oneroster_import_smoke.py`, `test_case_adapter_conformance.py`, plus `scripts/sign_compliance_bundle.py` that emits a HMAC-SHA256 manifest. Sources stay vendored, no new SaaS. |
| F5 | Branch-hygiene drift. | Branches: `refactor/pathfinder-learn-phase-0..4` exist as stacked merges; merge commits `ff759ce` (Phase 4), `4263cc0` (Phase 3), `bd54009` (Phase 2), and tips for Phase 1 (`fad9150`) and Phase 0 (`09ff1fc`) are unlabelled. Original prompt named `refactor/pathfinder-learn-phase-0`; work was stacked onto `phase-1`. | No tags were created when phases merged; reproducing the pilot demo currently requires reading the git graph by hand. | Annotated tags `pilot/phase-0..pilot/phase-4` at: `09ff1fc`, `fad9150`, `bd54009` (merge into phase-2 line), `4263cc0`, `ff759ce`. Push tags and document the mapping in this file (see §A.6 below). |
| F6 | Dead interactions in the UI. | See click-path audit §A.2. The primary offenders are `Start today's check-in` ([StudentLearningHome.tsx:307](../frontend/src/learning/routes/StudentLearningHome.tsx) — no `onClick`), the three `todaysPath` rows (same file, lines ~325–353), `Preview path` button (~380), nav links work fine, `Approve`/`Reject` only push to local in-memory list (no backend, no xAPI), `Send request` only pushes a string, Pathways `Compare` and `View details` and `Compare side-by-side` are no-ops, Trust & Safety `Export signed bundle`, `Run red-team probe`, `Manual rollback` are no-ops. | No real wiring was added when the Phase-1 shell was authored; PRD-fix-gaps-prompt §F6 captures this exactly. | Wire each click through the existing OSS modules: diagnostic via `LearningRepository`/`Diagnostic.next_item` (catsim adapter) + `BetaBKT` update; approvals via `LearningRepository` + `xAPIEmitter`; voice via the new `/ws/learning/voice`; KPI source via `/api/learning/kpis`; signed bundle via `scripts/sign_compliance_bundle.py`. All flows must work offline against `data/learning/jss2_maths_diagnostic_phase_2.json` and `data/learning/content_packs/`. |

## A.2 Click-path audit (F6)

Live evidence: Playwright snapshots at `/home`, `/teacher`, `/safety` on
2026-05-23T16:39Z confirm every row marked "no-op" below produced no URL
change, no network request, and no visible UI change beyond the local audit
list on the teacher page.

Legend for `Offline?`: **Y** = served from on-device fixtures/content
pack + IndexedDB queue; **N** = requires online round-trip; **Y+queue** =
runs locally, replays when online.

### Route: `/home` — `StudentLearningHome`

| Element | File:line | Current handler | Expected behaviour (architecture contract / PRD §7) | Target call | Offline? |
|---|---|---|---|---|---|
| `Start today's check-in` button | [StudentLearningHome.tsx:307](../frontend/src/learning/routes/StudentLearningHome.tsx#L307) | none (button has no `onClick`) | Open the JSS2 ratio diagnostic, first item from `jss2_maths_diagnostic_phase_2.json` selected via `catsim`; record xAPI `attempted`. | New `POST /api/learning/diagnostic/start` returning the first `DiagnosticItem`; navigate to in-app `/home/diagnostic/:sessionId`. | Y |
| `Ratio mini check-in` row | [StudentLearningHome.tsx:325](../frontend/src/learning/routes/StudentLearningHome.tsx#L325) | none | Same as above, scoped to the ratio skill. | Same endpoint, `skill_id=ratio-proportion`. | Y |
| `Fraction bar practice` row | StudentLearningHome.tsx (~340) | none | Same, `skill_id=fraction-operations`. | Same. | Y |
| `Exit ticket: scaling recipes` row | StudentLearningHome.tsx (~355) | none | Render exit-ticket items from content pack. | `GET /api/learning/exit_ticket/:id`. | Y |
| `Preview path` button | StudentLearningHome.tsx (~380) | none | Show next-week path preview. | Local nav to `/profile` (read-only, no backend call). | Y |
| Sidebar nav (`Learner` … `Trust & Safety`) | PathfinderLearnApp.tsx:225–253 | `NavLink` works | OK — no change. | n/a | Y |
| **Diagnostic answer buttons (do not exist yet)** | — | — | Each item answer click must call `POST /api/learning/diagnostic/answer`, update `MasteryEstimate` via `BetaBKT`, emit xAPI `answered`, and refresh radar/heatmap. | Build minimal `<DiagnosticItemCard />` under `routes/diagnostic/`. | Y+queue |

### Route: `/teacher` — `TeacherMasteryDashboard`

| Element | File:line | Current handler | Expected behaviour | Target call | Offline? |
|---|---|---|---|---|---|
| Filter badges (`All JSS2`, `Needs support`, …) | TeacherMasteryDashboard.tsx (~290) | `setActiveFilter` only | Re-filter `classRows`; no backend. | Local. | Y |
| `MultimodalIntentBar` text + `Send request` | PathfinderPhase2.tsx:152–192 | `pushEvent('Teacher request: …')` to local list only | Route through `IntentClassifier` → `PolicyGate` → `LearningPlanner.run_turn`; surface a draft `PendingApprovalPlanView`. | `POST /api/learning/intent` (server runs planner + validator; returns plan or refusal). | N (online-required; offline shows queued state) |
| `Approve` button | PathfinderPhase2.tsx:265 | `onApprove?.(planId)` then `pushEvent('Approved plan …')`; **no backend, no xAPI** | Move plan `pending → approved`, append `xAPIStatement` (verb `approved`), append `AuditEvent`. | `POST /api/learning/approvals/:planId/approve`. | Y+queue |
| `Reject` button | PathfinderPhase2.tsx:271 | `onReject?.(planId)`; same — local only | `pending → rejected` with reason; xAPI + audit. | `POST /api/learning/approvals/:planId/reject`. | Y+queue |

### Route: `/profile` — `StudentMasteryProfile`

| Element | File:line | Current handler | Expected behaviour | Target call | Offline? |
|---|---|---|---|---|---|
| `CounsellorGatePanel` Approve narration | PathfinderPhase3.tsx:259 | `onApproveNarration` → local list | Same approval pattern as F6 teacher; gate is counsellor, scope `career-plan-*`. | `POST /api/learning/career/:planId/approve_narration`. | Y+queue |
| `CounsellorGatePanel` Reject narration | PathfinderPhase3.tsx:266 | local only | Refusal recorded with rule id from `OrchestratorAdvisor`. | `POST /api/learning/career/:planId/reject_narration`. | Y+queue |
| Charts (`RadarChart`, `LineChart`) | StudentMasteryProfile.tsx:160–252 | static fixture | Drive from `MasteryEstimate` snapshot. | `GET /api/learning/profile/:studentId`. | Y |

### Route: `/pathways` — `PathwaysExplorer`

| Element | File:line | Current handler | Expected behaviour | Target call | Offline? |
|---|---|---|---|---|---|
| Search input + category badges | PathwaysExplorer.tsx (~210, ~260) | local filter via `useMemo` | OK; already correct. | n/a | Y |
| `Compare` button | PathwaysExplorer.tsx (~338) | `toggleCompare` local | OK as is; just needs explicit aria + max-3 badge. | n/a | Y |
| `View details` button | PathwaysExplorer.tsx (~345) | none | Open in-page pathway detail card (read-only sourced from `career/planner.py`). | Local route `/pathways/:id`, data from `data/learning/career/`. | Y |
| `Compare side-by-side` button | PathwaysExplorer.tsx (~358) | none | Open compare overlay. | Local component, no backend. | Y |

### Route: `/safety` — `TrustSafetyConsole`

| Element | File:line | Current handler | Expected behaviour | Target call | Offline? |
|---|---|---|---|---|---|
| KPI strip cards (6) | TrustSafetyConsole.tsx:264 | reads `pilotMetrics` fixture | Read from `/api/learning/kpis`; if `source == "fixture"` show a "fixture" badge per card. | `GET /api/learning/kpis`. | Y |
| `Export signed bundle` button | TrustSafetyConsole.tsx (~248) | none | Trigger `scripts/sign_compliance_bundle.py` (server-side endpoint) and download the resulting ZIP. | `POST /api/learning/evidence/export` returning a blob URL. | Y (signs local ledger slice) |
| `Run red-team probe` button | TrustSafetyConsole.tsx (~253) | none | Run `AdversarialProbe` set (`operations.load_adversarial_probes`) against the planner in dry-run mode. | `POST /api/learning/eval/red_team` returning per-probe outcomes. | Y (deterministic eval) |
| `Manual rollback` button | TrustSafetyConsole.tsx (~334) | none | Pull the kill-switch documented in arch contract §A.8 via `scripts/tenant_flags.py`. | `POST /api/learning/flags/rollback`; requires DPO role. | N |

## A.3 Acceptance criteria

| # | Acceptance command (run from repo root) | Pass criterion |
|---|---|---|
| F1 | `cd voicelive-api-salescoach-pathfinder-phase-0 && git status --porcelain` | Empty output, AND `git log --oneline -1` shows the new `feat(frontend): land PathfinderLearnApp shell and learning routes` commit on `fix/pathfinder-pilot-gaps-2026-05-23`. |
| F2 | `cd backend && pytest -k "kpis or operations"` then `cd ../frontend && npm run e2e -- --grep "kpi strip"` | Backend test asserts `GET /api/learning/kpis` returns a `PilotKPIReport` with `source: "fixture"` and the six metrics. Playwright asserts each KPI card renders the "fixture" badge until `source` flips. |
| F3 | `cd backend && pytest -k "voice"` and `cd ../frontend && npm run e2e -- --grep "voice check-in"` | Backend test exercises `FlaskSockVoiceTransportAdapter.handle_offline_frame` via the new `/ws/learning/voice` route and asserts `accepted=True, queued=True, offline_fallback="queued_multilingual_voice_frame"`. Playwright with `VITE_PATHFINDER_VOICE_ENABLED=true` clicks the new beta button and asserts the queued frame banner is shown; with the flag off the button is not rendered. |
| F4 | `python scripts/sign_compliance_bundle.py --out evidence/compliance/bundle.zip && cd backend && pytest -k "oneroster_smoke or case_adapter_conformance"` | Script produces `bundle.zip` + `bundle.zip.manifest.json` (HMAC-SHA256). Both pytest tests pass. `evidence/compliance/` contains the DPIA outline, manifest, and the signed ZIP. |
| F5 | `git tag --list 'pilot/*'` and `git ls-remote --tags origin 'pilot/*'` | Tags `pilot/phase-0..pilot/phase-4` exist locally and on `origin`, each pointing at the documented merge commit. |
| F6 | `cd frontend && npm run e2e -- --grep "pathfinder end-to-end"` | New Playwright spec: fresh page load → click `Start today's check-in` → answer one diagnostic item → the mastery cell for `ratio-proportion` increases on `/teacher` heatmap → `PendingApprovalCard` shows the planner suggestion → click `Approve` → `Audit events` list shows the action AND the `evidence/phase_2/trace_*.zip` updated by the test contains a schema-valid `xAPIStatement` with verb `approved`. Test runs with the dev server in offline mode (`VITE_PATHFINDER_OFFLINE=true`). |

## A.4 Out of scope for this fix pass

- Visual redesign of any surface or any token change in [theme/pathfinder-tokens.ts](../frontend/src/learning/theme/) (only minimal additions such as a "fixture" badge style if not already present).
- New top-level dependencies; specifically no Temporal, NestJS, Hermes, Keycloak, MinIO, no React/Vite/FluentUI bumps, no new SaaS vendor.
- Touching anything under `backend/src/services/`, `backend/src/routes/listening_eval_routes.py`, or any therapy domain (`exercise*`, `child*`, `pronunciation*`, etc.). Architecture contract A.1 import rule forbids it.
- Building a full WebSocket front-end audio path. Phase 3 voice remains "queue-then-sync"; F3 ships text-mode + offline queue only.
- Real labour-market data refresh; we keep the existing `labour_market_fixture` provenance.
- Production identity/RBAC for the new endpoints. They reuse the existing Flask session/JWT path; if a route requires an authenticated actor we follow the pattern in `API_CHILDREN_ENDPOINT` and stop. No new auth flow.
- Implementing AKS/IaC changes for the new endpoints — they slot under existing API Container App per arch contract A.5.

## A.5 Risk if a fix is wrong (and rollback)

| # | Worst-case if wrong | Rollback path |
|---|---|---|
| F1 | The first commit silently drops one of the untracked files (eg. a route). | `git reflog` → `git restore --source=<reflog-ref> -- frontend/src/learning`. Branch is brand-new, no shared history affected. |
| F2 | KPI endpoint returns wrong-shaped JSON, TrustSafetyConsole crashes. | Revert single commit; UI auto-falls back to the existing `pilotMetrics` fixture import (kept in place behind the new fetch as a safety net). |
| F3 | Voice WebSocket route leaks PII or 500s on connect. | Toggle `VITE_PATHFINDER_VOICE_ENABLED=false` (UI button hidden); remove the route registration in `app.py` (single line). No persisted state because the adapter only queues. |
| F4 | Signed bundle script has a wrong manifest and downstream verifiers reject it. | Re-run script with `--rebuild`; bundle ZIP is content-addressed by the manifest and not yet sent to any external party. |
| F5 | Tag placed on the wrong commit. | `git tag -d pilot/phase-X && git push origin :refs/tags/pilot/phase-X`; recreate. No code change. |
| F6 | A wired click invokes the planner with wrong scope and leaks plans across tenants. | Per arch contract A.4, RLS is the source-of-truth; cross-tenant queries return empty rows. Additionally guard each new endpoint with the same actor-scope assertion already used by `API_CHILDREN_ENDPOINT`. Roll back by reverting the F6 commit; the routes degrade to the current no-op state. |

## A.6 Tag → commit mapping for F5

| Tag | Target commit | Subject |
|---|---|---|
| `pilot/phase-0` | `09ff1fc` | feat: add pathfinder learn phase 0 foundation |
| `pilot/phase-1` | `fad9150` | feat: add pathfinder learn phase 1 foundations |
| `pilot/phase-2` | `bd54009` | Merge Pathfinder Learn Phase 2 |
| `pilot/phase-3` | `4263cc0` | Merge Pathfinder Learn Phase 3 |
| `pilot/phase-4` | `ff759ce` | Merge Pathfinder Learn Phase 4 |

Tags will be annotated (`git tag -a -m "Pathfinder pilot phase N reproducible point"`) and pushed with `git push origin --tags`.

---

## Decision summary (10 lines)

1. PRD-pathfinder-learn.md is missing from the repo; using `architecture-contract.md` as the binding contract surrogate — **needs explicit OK**.
2. Stage A produced this single doc; no code change yet, branch hygiene deferred to Stage B.0.
3. F1 strategy: cut `fix/pathfinder-pilot-gaps-2026-05-23` from `5824dc5`, commit all untracked UI as the first commit verbatim.
4. F2 strategy: add `/api/learning/kpis` (flat `@app.route`) backed by `operations.compute_kpi_report`, return `source: "fixture"`, UI badges it.
5. F3 strategy: add `/ws/learning/voice` flask-sock route around `FlaskSockVoiceTransportAdapter`; flag-gated text entry on Student Home, off by default.
6. F4 strategy: `evidence/compliance/` with DPIA outline, OneRoster smoke + CASE conformance tests, HMAC-SHA256 signed bundle script.
7. F5 strategy: five annotated `pilot/phase-N` tags on existing merge points; no rewrite of history.
8. F6 strategy: wire every dead click through new `/api/learning/*` endpoints reusing `LearningPlanner` + `PlanValidator` + `xAPIEmitter`; offline-first via fixtures and IndexedDB queue.
9. Out of scope: therapy/, deps bumps, voice audio path, new SaaS, theme redesign.
10. Worst case for every fix is reversible by a single revert/tag-delete/flag-flip; no destructive git, no shared infra touched.

## Contradictions surfaced

- **PRD link broken**: `docs/PRD-fix-gaps-prompt.md` points to `docs/PRD-pathfinder-learn.md`; that file is absent on this branch and on `origin/main`. Using the architecture contract instead. Decision needed before Stage B.
- **Branch name**: prompt headers say `refactor/pathfinder-learn-phase-1` is current; correct (`HEAD -> refactor/pathfinder-learn-phase-1`). The historical execution prompt text "branch off phase-0" is a stale reference, captured under F5.
- **`pilot/phase-2` merge commit**: the visible merge into the Phase-2 line is `bd54009 Merge Pathfinder Learn Phase 2`; an alternate read of "merge point" could be `5ab5240` (tip of phase-2). The plan tags the merge commit so each tag dereferences to a stable, deployable tree. Confirm before tagging.
- **`PathfinderPhase3.test.tsx` and `PathfinderPhase2.test.tsx` exist** and would also need updating if approve/reject signatures change in F6. Plan keeps the existing callback shape (`onApprove(planId)`) and only adds backend-call wiring behind it, so existing tests stay green.
